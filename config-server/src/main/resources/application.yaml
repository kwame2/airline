server:
  port: 5000

spring:
  cloud:
    config:
      name: config
      server:
        git:
          uri: https://github.com/kwame2/airline/config-files
          search-paths: config-files*
  application:
    name: config